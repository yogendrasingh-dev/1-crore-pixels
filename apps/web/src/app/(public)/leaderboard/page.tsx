import { LeaderboardList } from "./LeaderboardList";

export default function LeaderboardPage() {
  return (
    <main>
      <h1>Community Leaderboard</h1>
      <p>A thank-you, not a payout — ranked by verified referrals, not cash commissions.</p>
      <LeaderboardList />
    </main>
  );
}
