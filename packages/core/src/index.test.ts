import { describe, expect, it } from "vitest";
import * as core from "./index";

describe("@1crore-pixels/core scaffold", () => {
  it("loads without throwing", () => {
    expect(core).toBeDefined();
  });
});
